#include <stdio.h>
#include <windows.h>
// Criação do função navegando
void navegando(){
	// Variável que foi usada no comando for
	int  i;
	for (i =0 ; i<12; i++){
		printf("NAVEGANDO...\n");
	// Comando sleep() foi usado para definir a duração do laço de repetição (1 segundo)
		sleep(1);
	}
}
